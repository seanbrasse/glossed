# Xcode / Swift
*.xcodeproj
xcuserdata/
DerivedData/
.build/
.swiftpm/
*.playground

# Env + secrets
.env
.env.*
!.env.example

# Supabase local
supabase/.branches/
supabase/.temp/
supabase/.env

# macOS
.DS_Store

# Tooling
.claude/settings.local.json

# Session-local scheduler artifacts
.claude/scheduled_tasks.lock
.claude/*.lock

# GLO-223: the local catalog snapshot. 6MB of import-script output, not source —
# a fresh machine builds it with HANDOFF §9's scripts, this is the fast path back
# after a `supabase db reset`.
supabase/.catalog-snapshot.sql

# Generated by xcodegen alongside the .xcodeproj (project.yml `info:`).
app/Glossed/Info.plist

# Device builds (make run uses .build for the simulator).
.build-device/
